#!/usr/bin/python3.13
import clear
clear

# No programa a seguir, inverta as linhas do if e else, negando a condição. 
# Adicione as linhas necessárias para funcionar em python.

a = int(input(" Informe o valor de A: "))
b = int(input(" Informe o valor de B: "))

if (a > b):

    print (f"{b} é maior do que {a}.")

else:

    print (f"{a} é maior do que {b}")