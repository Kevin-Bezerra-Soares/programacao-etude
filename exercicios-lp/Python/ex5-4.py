#!/usr/bin/python3.13
import clear
clear

# Modifique o prigrama anterior para imprimir de 1 até o número digitado pelo usuário,
# mas, desta vez, apenas números ímpares.

fim = int(input("\n Digite o último número a imprimir: "))

x = 1

while (x <= fim):

    print (x)
    x += 2
